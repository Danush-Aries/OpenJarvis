# Core runtime dependencies — kept in sync with pyproject.toml [project.dependencies].
# Install with:   pip install -r requirements.txt
# Or (preferred): uv sync

click>=8
datasets>=4.5.0
ddgs>=9.11.4
httpx>=0.27
openai>=1.30
posthog>=3.0
nvidia-ml-py>=12.560.30
python-telegram-bot>=22.6
rich>=13
tomlkit>=0.12

# Python < 3.11 needs the backport of tomllib
tomli>=2.0; python_version < '3.11'

# Server extras (required to run app.py / uvicorn app:app)
fastapi>=0.110
uvicorn>=0.30
pydantic>=2.0
python-multipart>=0.0.9
